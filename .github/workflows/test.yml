name: test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Test (node.js 12 on ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0
      - name: Use node.js 12
        uses: actions/setup-node@v3.2.0
        with:
          node-version: 12
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build --if-present
      - name: Run tests
        run: npm run test
